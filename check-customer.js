const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const c = await prisma.customer.findUnique({ where: { id: 'cmn9vl29v0000thwku8so7rff' } }); console.log('Customer:', c ? c.name : 'Not found'); const u = await prisma.user.findFirst({ where: { role: 'ADMIN' }, include: { permissionGroup: true } }); console.log('Admin user:', u.name, 'Permissions:', u.permissions, 'Group:', u.permissionGroup?.permissions); } main().finally(() => prisma.());
